1. Add pre-commit
2. Implement elapsed time
3. log start and end time
4. Implement hybrid, scalable solution that automatically switches between Pandas (for small files) and PySpark (for large files).
5. Create a global config file contains module based configs. (need to check preferred option b/w single vs multiple configs).
6. Log Rotation
7. Add execution timing decorators